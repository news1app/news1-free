<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_news1"
        android:roundIcon="@drawable/ic_news1"
        android:label="@string/app_name"
        android:theme="@style/Theme.NEWS1"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true">

        <activity android:name=".MenuActivity" />
        <activity android:name=".MarketsActivity" />
        <activity android:name=".NewsActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".AnalysisActivity" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".SourcesActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".HistoryActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
